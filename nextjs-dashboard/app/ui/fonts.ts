import { Afacad_Flux } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const afacad_flux = Afacad_Flux ({ subsets: ['latin']});
export const lusitana = Lusitana ({
    subsets: ['latin'],
    weight: ['400', '700']
});